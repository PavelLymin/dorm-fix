name: ui_kit
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0
resolution: workspace

environment:
  sdk: ^3.11.0-16.0.dev

dependencies:
  flutter:
    sdk: flutter
  # Fonts
  google_fonts: ^6.3.2

  # Yandex MapKit SDK
  # yandex_maps_mapkit_lite: ^4.25.0-beta

  # MapKit that allows you to use the features of Yandex.Maps
  yandex_maps_mapkit: ^4.25.0-beta


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true